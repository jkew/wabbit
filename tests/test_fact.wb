// Compute factorials

func fact(n) {
   var x = 1;
   var result = 1;
   while x < n {
       result = result * x;
       x = x + 1;
   }
   return result * n;
}

var x = 1;
while x < 10 {
    print fact(x);
    x = x + 1;
}
